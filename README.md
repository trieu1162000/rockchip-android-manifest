# rockchip-android-manifest
## Usage

To set up the repository with the desired manifest for version `tinker_board_2-android11-2.0.9`, use the following commands:

```shell
repo init -u https://github.com/trieu1162000/rockchip-android-manifest.git -b android11-rockchip -m LAE_android11-2.0.9.xml
repo sync